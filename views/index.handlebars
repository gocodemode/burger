<div class="container">
  <div class="row center">
    <div class="col-sm-12 text-center">
      <h1>Eat-da-Burger!</h1>
      <img src="/assets/img/burger.png" alt="spongebob to the rescue">
    </div>
  </div>
  <div class="row">
    <div class="col-sm-6">
      <h2>Burgers ready to be eaten!</h2>
        <ul>
          {{#each burgers}}
            {{#unless devoured}}
              {{> burgers/burger-block devoured=true}}
            {{/unless}}
          {{/each}}
        </ul>
    </div>
    <div class="col-sm-6">
      <h2>Burgers already devoured!</h2>
      <ul>
        {{#each burgers}}
          {{#if devoured}}
            {{> burgers/burger-block devoured=false}}
          {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
  <div class="row">
    <h2>Add a Burger</h2>
      <form class="create-form">
        <div class="form-group">
          <label for="ca">Burger Name:</label>
          <input type="text" id="ca" name="name">
        </div>
        <div class="form-group">
          <label for="devoured">Devour?</label><br>
          <input type="radio" name="devoured" value="1" checked>Devoured!<br>
          <input type="radio" name="devoured" value="0">Eat the Burger.
        </div>
        <button type="submit">Add Burger</button>
      </form>
  </div>
</div>