<li>
	{{burger_name}}

	<button class="change-satisfaction" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}Devoured!{{else}}Try it Out!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Delete!</button>
</li>